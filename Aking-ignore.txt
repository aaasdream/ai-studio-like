# 這是一個根據您當前選擇產生的忽略檔案 (Aking-ignore.txt)
# 將此檔案放在您要讀取的資料夾的根目錄下，下次讀取時將會自動套用。
# 每一行代表一個要忽略的檔案或資料夾名稱 (會遞迴忽略其下所有內容)。
# 以 # 開頭的行是註解，會被忽略。
#
# --- 範例規則 ---
# node_modules
# .env
# dist
# build
#
# --- 您目前未勾選的項目 ---
dist
node_modules
.env
build